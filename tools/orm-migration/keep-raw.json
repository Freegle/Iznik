{
  "_comment": [
    "Deliberate keep-raw decisions, per plan section 7.5. These are the sites",
    "where forcing the ORM would hurt readability and a rewrite bug would be",
    "silent and expensive. Every rule must carry a reason: the plan forbids",
    "silent deferral, so a site can only leave the raw count by being converted",
    "or by appearing here with a justification that survived review.",
    "",
    "Rules match on file plus optional function. The extractor applies them on",
    "every regeneration, so these decisions are declarative rather than a",
    "hand-edited state that drifts.",
    "",
    "Each keep-raw site becomes an input to migration Phase 4 (the dialect",
    "port), and per 7.5 should sit behind a thin repository layer with its own",
    "integration test."
  ],
  "rules": [
    {
      "file": "iznik-server-go/message/bounds.go",
      "function": "Bounds",
      "reason": "Two 20-line spatial/CASE/ANY_VALUE queries over the messages_spatial index. A rewrite bug here silently shows the wrong nearby posts, which no test would obviously catch. Named in plan 7.5.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "file": "iznik-server-go/message/search.go",
      "reason": "Embeds utils.AuthorReachCapWhere, the most widely reused WHERE fragment in the codebase: JSON extraction plus haversine trigonometry plus spatial predicates. Named in plan 7.5. Note that reachcap.go itself builds a string and never executes it, so the cost lands on the callers that splice it in.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "file": "iznik-server-go/isochrone/message.go",
      "reason": "Embeds utils.AuthorReachCapWhere; see the note on message/search.go. Named in plan 7.5.",
      "porting": "PostgreSQL: the hard one, and named in plan 7.5. AuthorReachCapWhere combines three things that each translate differently. JSON_EXTRACT / JSON_UNQUOTE become ->> or #>> and are not interchangeable with MySQL's on NULL handling. The haversine arithmetic is plain SQL and ports as-is, but should be replaced by ST_DWithin on geography, which is both correct and index-assisted. The spatial predicates follow the spatial note. Because this fragment is spliced into many callers, port it once behind the repository layer plan 7.5 asks for, and give it its own integration test before any caller moves.",
      "category": "Reach-cap WHERE fragment (plan 7.5 named)"
    },
    {
      "file": "iznik-server-go/ormharness/",
      "reason": "Migration harness. These sites execute the original raw SQL by design, in order to compare it against the replacement ORM call, so they are not migration targets.",
      "porting": "Not applicable: this is the migration harness itself, not production code. Its raw SQL exists to compare against production SQL and is deliberately unconverted.",
      "category": "Not applicable: migration harness"
    },
    {
      "file": "iznik-server-go/userdump/sqlite.go",
      "reason": "Builds a SQLite export file, not a query against the Percona cluster. Out of scope for the MySQL-to-ORM work and unaffected by the engine migration.",
      "porting": "Not applicable: this writes SQLite, for the user data export. It is unaffected by the MySQL-to-PostgreSQL migration and should not be counted in the Phase 4 estimate.",
      "category": "Not applicable: SQLite export"
    },
    {
      "file": "iznik-server-go/recommendations/stats.go",
      "function": "Stats",
      "reason": "Carries an optimiser hint, /*+ MAX_EXECUTION_TIME(10000) */, over messages_likes (~75M rows, and unindexed for this access pattern). The caller inspects .Error to set a degraded flag when the hint aborts a slow query, so the hint is load-bearing behaviour rather than a comment. It could probably be smuggled into the Select string, since GORM renders SELECT <select-clause> FROM, but getting that subtly wrong on a table this size degrades a live endpoint rather than failing visibly, so it wants a deliberate decision and its own test rather than a wave conversion.",
      "porting": "PostgreSQL: MAX_EXECUTION_TIME has no equivalent as a statement hint. Use SET LOCAL statement_timeout inside the transaction, which is per-transaction rather than per-statement. The hint is load-bearing here - it exists so a slow stats query cannot monopolise a connection - so it must be carried across, not dropped.",
      "category": "MAX_EXECUTION_TIME hint"
    },
    {
      "file": "iznik-server-go/userdump/",
      "reason": "The dump pipeline threads a raw *sql.DB rather than a *gorm.DB through buildDump/buildPlan/scanIDs/runDBSpec, and two of its four query sites build dynamic SQL text that was never extracted. Converting the other two would split one pipeline's connection handling across two patterns and needs a signature change, which is a redesign rather than a conversion. Consistent with the existing userdump/sqlite.go entry.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "file": "iznik-server-go/ormshadow/",
      "reason": "Layer 3 harness infrastructure. Reads the shadow batch flag from the config table by design and is not a migration target, in the same way the ormharness package is not.",
      "porting": "Not applicable: migration harness, as above.",
      "category": "Not applicable: migration harness"
    },
    {
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "GetOrCreateUser2ModChat",
      "reason": "The SELECT ... FOR UPDATE runs through tx.QueryRow on a raw *sql.Tx taken from db.DB().Begin(), and shares that transaction with a following raw INSERT that needs sql.Result.LastInsertId() on the same connection. Converting only the SELECT would either take it outside the transaction, defeating the row lock, or force the whole function onto db.Transaction(). Same shape as tryst.CreateTryst: a per-site redesign, not a mechanical conversion. Found during the Wave 1 chat batch.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "file": "iznik-server-go/message/message.go",
      "function": "findOrCreateUserForDraft",
      "reason": "INSERT whose caller needs LastInsertId from the exact connection that ran it. See the shared note on CreatePartnerUser below: GORM's map-valued Create writes the new id back under the literal key \"@id\" when no schema is parsed, which is undocumented, contradicts GORM's own 'Create From Map' documentation, and has no coverage in GORM's test suite. Found by the Wave 2 write pilot.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "file": "iznik-server-go/user/partner.go",
      "function": "CreatePartnerUser",
      "reason": "INSERT whose caller needs LastInsertId. These sites use sqlDB.Exec deliberately so the id is read from the MySQL protocol response on the very connection that performed the insert. The hazard being avoided is NOT replication lag: LAST_INSERT_ID() is connection-scoped session state, so a follow-up SELECT can be handed a different pooled connection and return 0 or another session's id, as the comment in session/social_auth.go already spells out. Converting to GORM's Create would instead depend on primary-key writeback that, traced through callbacks/create.go in v1.31.0, stores the id under the literal key \"@id\" when stmt.Schema is nil - real behaviour, but undocumented, contradicted by the public docs, and untested upstream. Relying on that for the one value these sites exist to protect is precisely the silent, expensive rewrite bug plan 7.5 reserves keep-raw for. database/insert.go's ExecInsertGetID already encapsulates this pattern and is itself recorded as non-migration infrastructure; pointing these call sites at it would be a sensible dedup, but that is a refactor rather than an ORM conversion.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "file": "iznik-server-go/export/export.go",
      "function": "PostExport",
      "reason": "INSERT whose caller needs LastInsertId; see the note on CreatePartnerUser. Found by the Wave 2 write pilot.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "file": "iznik-server-go/tryst/tryst.go",
      "function": "CreateTryst",
      "reason": "Deliberately drops to the database/sql handle so that LastInsertId() reports the row the upsert actually touched, and returns it to the caller. GORM's Create does not surface LastInsertId for an ON DUPLICATE KEY UPDATE, and the read/write split makes reading the id back with a follow-up SELECT unsafe. Found during the Wave 0 pilot; needs a per-site redesign rather than a mechanical conversion.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "file": "iznik-server-go/database/retry.go",
      "reason": "Infrastructure wrapper: takes an already-built statement from a caller and retries it. The SQL belongs to the calling site, which is inventoried separately.",
      "porting": "PostgreSQL: the retry predicates match MySQL error numbers (1213 deadlock, 1205 lock wait timeout, 2006/2013 connection loss). Postgres uses SQLSTATE instead - 40P01 deadlock_detected, 55P03 lock_not_available, class 08 for connection failures. This is infrastructure rather than a query, but it must be ported or retries silently stop working, which shows up as user-facing errors under load rather than as a test failure.",
      "category": "Retry predicates (infrastructure)"
    },
    {
      "file": "iznik-server-go/database/insert.go",
      "reason": "Infrastructure wrapper around LAST_INSERT_ID handling for the read/write split. The SQL belongs to the calling site, which is inventoried separately.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "02f8f0aee316",
      "file": "iznik-server-go/team/team.go",
      "function": "PostTeam",
      "reason": "PostTeam() inserts into teams and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "050e3574aa6f",
      "file": "iznik-server-go/message/bulkItem.go",
      "function": "findOrCreateUser2UserRoom",
      "reason": "findOrCreateUser2UserRoom() inserts into chat_rooms and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "0adadbabde5b",
      "file": "iznik-server-go/volunteering/volunteering.go",
      "function": "Create",
      "reason": "Create() inserts into volunteering and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "10bcbd6a6404",
      "file": "iznik-server-go/newsfeed/newsfeed.go",
      "function": "createRefer",
      "reason": "createRefer() inserts into newsfeed and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "123512413259",
      "file": "iznik-server-go/message/helper.go",
      "function": "helperSendAction",
      "reason": "helperSendAction() inserts into helper_repliers and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "132b1f639e73",
      "file": "iznik-server-go/stdmsg/stdmsg.go",
      "function": "PostStdMsg",
      "reason": "PostStdMsg() inserts into mod_stdmsgs and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "1590b130f529",
      "file": "iznik-server-go/message/message.go",
      "function": "PutMessageAs",
      "reason": "PutMessageAs() inserts into messages and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "181d8342ea4a",
      "file": "iznik-server-go/modconfig/modconfig.go",
      "function": "PostModConfig",
      "reason": "PostModConfig() inserts into mod_configs and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "1f5798e8214d",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "handleNudge",
      "reason": "handleNudge() inserts into chat_messages and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "20dfce4d2228",
      "file": "iznik-server-go/spammers/spammers.go",
      "function": "PostSpammer",
      "reason": "PostSpammer() inserts into spam_users and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "24704432f4d4",
      "file": "iznik-server-go/user/user.go",
      "function": "PutUser",
      "reason": "PutUser() inserts into users and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "258437a60f5d",
      "file": "iznik-server-go/donations/giftaid.go",
      "function": "SetGiftAid",
      "reason": "SetGiftAid() inserts into giftaid and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "26c94565517d",
      "file": "iznik-server-go/message/message.go",
      "function": "createSystemChatMessage",
      "reason": "createSystemChatMessage() inserts into chat_rooms and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "322b611c86cc",
      "file": "iznik-server-go/shortlink/shortlink.go",
      "function": "PostShortlink",
      "reason": "PostShortlink() inserts into shortlinks and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "3efeb225c001",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "GetOrCreateUser2UserChat",
      "reason": "GetOrCreateUser2UserChat() inserts into chat_rooms and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "42bb2fc5fe91",
      "file": "iznik-server-go/noticeboard/noticeboard.go",
      "function": "PostNoticeboard",
      "reason": "PostNoticeboard() inserts into noticeboards and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "45b8b0bc2060",
      "file": "iznik-server-go/communityevent/communityEvent.go",
      "function": "Create",
      "reason": "Create() inserts into communityevents and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "45c97c1f1f58",
      "file": "iznik-server-go/auth/auth.go",
      "function": "CreateSessionAndJWT",
      "reason": "CreateSessionAndJWT() inserts into sessions and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "47417e0f74d7",
      "file": "iznik-server-go/location/location.go",
      "function": "CreateLocation",
      "reason": "CreateLocation() inserts into locations and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "4aef5392a01f",
      "file": "iznik-server-go/message/helper.go",
      "function": "helperUpsertReplier",
      "reason": "helperUpsertReplier() inserts into helper_repliers and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "4d2e18cf27e3",
      "file": "iznik-server-go/merge/merge.go",
      "function": "CreateMerge",
      "reason": "CreateMerge() inserts into merges and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "54e869591bc4",
      "file": "iznik-server-go/alert/alert.go",
      "function": "CreateAlert",
      "reason": "CreateAlert() inserts into alerts and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "65574169749a",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "PutChatRoom",
      "reason": "PutChatRoom() inserts into chat_rooms and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "659e00792d43",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "PutChatRoom",
      "reason": "PutChatRoom() inserts into chat_rooms and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "7394291c903d",
      "file": "iznik-server-go/message/helper.go",
      "function": "helperCreateProposal",
      "reason": "helperCreateProposal() inserts into helper_proposals and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "7e3e5f4d0b36",
      "file": "iznik-server-go/message/helper.go",
      "function": "ensureBatchRow",
      "reason": "ensureBatchRow() inserts into helper_batches and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "7ec60cde5d39",
      "file": "iznik-server-go/message/helper.go",
      "function": "helperSendAction",
      "reason": "helperSendAction() inserts into helper_proposals and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "88399aa73e56",
      "file": "iznik-server-go/admin/admin.go",
      "function": "PostAdmin",
      "reason": "PostAdmin() inserts into admins and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "8cbeeeb7e32f",
      "file": "iznik-server-go/group/group.go",
      "function": "CreateGroup",
      "reason": "CreateGroup() inserts into groups and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "90b0f0bb3029",
      "file": "iznik-server-go/newsfeed/create.go",
      "function": "CreateNewsfeedEntry",
      "reason": "CreateNewsfeedEntry() inserts into newsfeed and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "b0445c89f59e",
      "file": "iznik-server-go/image/image.go",
      "function": "doCreate",
      "reason": "doCreate() inserts into its target table and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "bbbc465b075c",
      "file": "iznik-server-go/session/social_auth.go",
      "function": "socialMatchOrCreate",
      "reason": "socialMatchOrCreate() inserts into users and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "c9c7922e5379",
      "file": "iznik-server-go/user/user.go",
      "function": "PutUser",
      "reason": "PutUser() inserts into sessions and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "d3790f53ec52",
      "file": "iznik-server-go/message/helper.go",
      "function": "insertHelperChat",
      "reason": "insertHelperChat() inserts into chat_messages and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "dfc3d2fdb2ba",
      "file": "iznik-server-go/message/helper.go",
      "function": "helperSetItemState",
      "reason": "helperSetItemState() inserts into helper_item_states and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "f6190b74d8d5",
      "file": "iznik-server-go/story/story.go",
      "function": "CreateStory",
      "reason": "CreateStory() inserts into users_stories and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "f961504c334d",
      "file": "iznik-server-go/newsfeed/newsfeed.go",
      "function": "createPost",
      "reason": "createPost() inserts into newsfeed and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "faa9018435a3",
      "file": "iznik-server-go/message/bulkItem.go",
      "function": "upsertBulkItems",
      "reason": "upsertBulkItems() inserts into messages_bulk_items and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "fb88302d28cb",
      "file": "iznik-server-go/message/bulkItem.go",
      "function": "ingestBulkItemPhotos",
      "reason": "ingestBulkItemPhotos() inserts into messages_attachments and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "1571f00a4ce8",
      "file": "iznik-server-go/image/image.go",
      "function": "doCreate",
      "reason": "doCreate() inserts into its target table and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "40de8b0d3f98",
      "file": "iznik-server-go/comment/comment.go",
      "function": "Create",
      "reason": "Create() inserts into users_comments and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "58cb0a225ebe",
      "file": "iznik-server-go/amp/amp.go",
      "function": "PostChatReply",
      "reason": "PostChatReply() inserts into chat_messages and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "6b9a23982cc9",
      "file": "iznik-server-go/modconfig/modconfig.go",
      "function": "PostModConfig",
      "reason": "PostModConfig() inserts into mod_configs and then reads the generated id back with LastInsertId() on the same connection. LAST_INSERT_ID() is connection-scoped session state, so the read is only correct while it shares a connection with the write. Converting this would move the id onto GORM map-Create's undocumented \"@id\" writeback and hand the connection choice to the dbresolver plugin. The failure mode is not an error: it is a row created against the wrong parent id, found days later. Enforced by ratchet gate (k); see tools/orm-migration/lastinsertid-sites.json.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "e137c396bd13",
      "file": "iznik-server-go/modconfig/modconfig.go",
      "function": "PostModConfig",
      "reason": "INSERT ... SELECT: PostModConfig copies the bulkops rows of the template config into the new one in a single statement. GORM has no builder that expresses INSERT ... SELECT as one atomic statement - Create takes values, not a query - so converting it would mean reading the rows and inserting them separately, which is a different statement with a different concurrency story, not a port. No INSERT ... SELECT site anywhere in the manifest has been converted; they are all still raw. Note this is NOT a LastInsertId site: an earlier version of check-lastinsertid.sh flagged it by proximity, because the LastInsertId in this function belongs to the mod_configs INSERT below it, not to this one.",
      "porting": "PostgreSQL: INSERT ... RETURNING id. This is the one category that gets EASIER on Postgres - RETURNING is part of the statement, so the id comes back with the insert and the connection-scoped LAST_INSERT_ID() problem disappears entirely. GORM exposes it via clause.Returning, so these sites may become convertible during the port rather than staying raw.",
      "category": "INSERT whose generated id is read back"
    },
    {
      "id": "08d12a748d01",
      "file": "iznik-server-go/newsfeed/newsfeed.go",
      "function": "Post",
      "reason": "INSERT ... SELECT: copies the message attachment rows into the newsfeed post in one statement. GORM has no builder that keeps INSERT ... SELECT atomic - Create takes values, not a query - so a conversion would become a read followed by a separate insert, which is a different statement with a different concurrency story. No INSERT ... SELECT site anywhere in the manifest has been converted.",
      "porting": "PostgreSQL: INSERT INTO ... SELECT is standard and ports unchanged, but check any ON DUPLICATE KEY UPDATE tail, which becomes ON CONFLICT ... DO UPDATE and needs a named unique index or constraint on the conflict target - Postgres will not infer it.",
      "category": "INSERT ... SELECT"
    },
    {
      "id": "c4e30fd6a513",
      "file": "iznik-server-go/noticeboard/noticeboard.go",
      "function": "PatchNoticeboard",
      "reason": "Dynamic SQL (manifest records dynamic: true). The position value is assembled with fmt.Sprintf, embedding lng, lat and the SRID as literal text into the statement before it is ever executed, so the recorded goldenSql is the pre-format Go source text rather than SQL. There is nothing a GORM render could be compared against, which makes Layer 1 parity impossible for this site by construction rather than by difficulty. Plan 7.4 requires a passing parity test before a site counts as converted, so it stays raw.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "92416198b77d",
      "file": "iznik-server-go/authority/authority.go",
      "function": "Single",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "3048dd76eab0",
      "file": "iznik-server-go/authority/authority.go",
      "function": "Single",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "1cbaddb2ed2a",
      "file": "iznik-server-go/authority/message.go",
      "function": "Messages",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "58213463d5dc",
      "file": "iznik-server-go/authority/stats.go",
      "function": "GetStatsByAuthority",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "88aea8da62be",
      "file": "iznik-server-go/dashboard/dashboard.go",
      "function": "GetDashboard",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "2811b4d3acf7",
      "file": "iznik-server-go/group/group.go",
      "function": "GetGroup",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "547458a591ae",
      "file": "iznik-server-go/group/group.go",
      "function": "getMultipleGroups",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "6d0982e798b5",
      "file": "iznik-server-go/group/group.go",
      "function": "validateGeometry",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "548090e97d00",
      "file": "iznik-server-go/group/group.go",
      "function": "PatchGroup",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "701348841f78",
      "file": "iznik-server-go/isochrone/isochrone.go",
      "function": "ListIsochrones",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "bff19d769e26",
      "file": "iznik-server-go/isochrone/isochrone.go",
      "function": "ListIsochrones",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "34e1f2e54b22",
      "file": "iznik-server-go/isochrone/isochrone.go",
      "function": "EnsureIsochroneExists",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "5eea52bb68f7",
      "file": "iznik-server-go/isochrone/isochrone.go",
      "function": "EnsureIsochroneExists",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "e0e7375e71aa",
      "file": "iznik-server-go/isochrone/isochrone.go",
      "function": "EnsureIsochroneExists",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "583d5d5394ad",
      "file": "iznik-server-go/isochrone/isochrone.go",
      "function": "HealPointIsochrones",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "498e74a4e8c5",
      "file": "iznik-server-go/message/postmatches.go",
      "function": "PostMatches",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "7e7e69fa2f85",
      "file": "iznik-server-go/message/reach.go",
      "function": "ReachBlockedSet",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "388f473a3332",
      "file": "iznik-server-go/message/reach.go",
      "function": "Reach",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "e9346f66bbf4",
      "file": "iznik-server-go/noticeboard/noticeboard.go",
      "function": "getList",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "23ee2bc0640f",
      "file": "iznik-server-go/town/town.go",
      "function": "Near",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "c42088f47c61",
      "file": "iznik-server-go/newsfeed/newsfeed.go",
      "function": "getFeed",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "7558e63cd067",
      "file": "iznik-server-go/newsfeed/newsfeed.go",
      "function": "getFeed",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (ST_AsText/ST_Y/ST_X/ST_CENTROID/ST_Contains/ST_GeometryType/ST_Intersects/ST_SIMPLIFY/ST_GeomFromText/ST_AsGeoJSON/MBRContains/ST_Distance_Sphere or similar), the category plan 7.5 names explicitly. A rewrite bug here changes which posts/areas show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "3d5506803f0c",
      "file": "iznik-server-go/admin/admin.go",
      "function": "ListAdmins",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "f1e9e49a9c89",
      "file": "iznik-server-go/comment/comment.go",
      "function": "Get",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "31fea9e6f321",
      "file": "iznik-server-go/donations/donations.go",
      "function": "GetDonations",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "15d5998c44f2",
      "file": "iznik-server-go/embedding/store.go",
      "function": "fetchEntries",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "bc2f944bfbb7",
      "file": "iznik-server-go/image/image.go",
      "function": "ownsImageParent",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "6f9c3996f035",
      "file": "iznik-server-go/image/image.go",
      "function": "doRotate",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "2ad46344c8b2",
      "file": "iznik-server-go/image/image.go",
      "function": "doRotate",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "1be407fe0a15",
      "file": "iznik-server-go/image/image.go",
      "function": "Get",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "1606033fd8f7",
      "file": "iznik-server-go/image/image.go",
      "function": "Get",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "bc3af5374c0c",
      "file": "iznik-server-go/job/job.go",
      "function": "JobsForIDs",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "6cf1b5aded22",
      "file": "iznik-server-go/logs/logs.go",
      "function": "GetLogs",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "2c3b155f346b",
      "file": "iznik-server-go/membership/membership.go",
      "function": "GetMemberships",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "836dc8807739",
      "file": "iznik-server-go/membership/membership.go",
      "function": "GetMemberships",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "5f742c0fcf1f",
      "file": "iznik-server-go/membership/membership.go",
      "function": "GetMemberships",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "bbc55cf96110",
      "file": "iznik-server-go/membership/membership.go",
      "function": "GetMemberships",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "5f6ca1b9022f",
      "file": "iznik-server-go/membership/membership.go",
      "function": "GetMemberships",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "fdd14a1656c7",
      "file": "iznik-server-go/membership/membership.go",
      "function": "getSpamMembers",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "3119115f3abe",
      "file": "iznik-server-go/membership/membership.go",
      "function": "getHappinessMembers",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "1a000d04649b",
      "file": "iznik-server-go/membership/membership.go",
      "function": "getVisibleRatings",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "bc6d923b540d",
      "file": "iznik-server-go/message/groups.go",
      "function": "Groups",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "7ba4875e8aec",
      "file": "iznik-server-go/message/helper.go",
      "function": "helperUpsertReplier",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "b48b319835d0",
      "file": "iznik-server-go/message/helper.go",
      "function": "helperSetItemState",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "bfe25b4914e8",
      "file": "iznik-server-go/message/message_list.go",
      "function": "ListMessages",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "a716e6f7dc57",
      "file": "iznik-server-go/message/message_list.go",
      "function": "ListMessagesMT",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "e1228c35e4d2",
      "file": "iznik-server-go/message/message_list.go",
      "function": "ListMessagesMT",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "d6cc20019a01",
      "file": "iznik-server-go/message/message_list.go",
      "function": "ListMessagesMT",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "7b739452734d",
      "file": "iznik-server-go/message/message_list.go",
      "function": "ListMessagesMT",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "6358402174f8",
      "file": "iznik-server-go/message/message_list.go",
      "function": "ListMessagesMT",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "80c36f2da91e",
      "file": "iznik-server-go/microvolunteering/microvolunteering.go",
      "function": "GetChallenge",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "309561e40e15",
      "file": "iznik-server-go/microvolunteering/microvolunteering.go",
      "function": "getPendingMessageChallenge",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "bde82a974f05",
      "file": "iznik-server-go/microvolunteering/microvolunteering.go",
      "function": "getApprovedMessageChallenge",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "ff5193d35cf8",
      "file": "iznik-server-go/microvolunteering/microvolunteering.go",
      "function": "getPhotoRotateChallenge",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "3762cb36efcf",
      "file": "iznik-server-go/microvolunteering/microvolunteering.go",
      "function": "listMicroActions",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "a7b00c5503b7",
      "file": "iznik-server-go/modconfig/modconfig.go",
      "function": "PatchModConfig",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "d80ab5badcb6",
      "file": "iznik-server-go/newsfeed/newsfeed.go",
      "function": "RecentNonAlertNewsfeedIDs",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "f382f9bfe80b",
      "file": "iznik-server-go/rippling/analytics.go",
      "function": "fetchDriveSample",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "2def63211a50",
      "file": "iznik-server-go/rippling/analytics.go",
      "function": "Analytics",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "62cea0b491c9",
      "file": "iznik-server-go/rippling/analytics.go",
      "function": "Analytics",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "1dee90c3c378",
      "file": "iznik-server-go/rippling/analytics.go",
      "function": "trendSeries",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "791c683326a8",
      "file": "iznik-server-go/rippling/analytics.go",
      "function": "rippledOutSection",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "8cdbd33d1052",
      "file": "iznik-server-go/rippling/analytics.go",
      "function": "rippledOutSection",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "568a5645fba7",
      "file": "iznik-server-go/rippling/metrics.go",
      "function": "Metrics",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "10ee37c98574",
      "file": "iznik-server-go/rippling/metrics.go",
      "function": "Metrics",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "f43d5f680ef9",
      "file": "iznik-server-go/session/session.go",
      "function": "GetSession",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "3f3696f3bba4",
      "file": "iznik-server-go/session/session.go",
      "function": "GetSession",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "76555fe088e5",
      "file": "iznik-server-go/session/session.go",
      "function": "GetSession",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "f85b0b8ed693",
      "file": "iznik-server-go/session/session.go",
      "function": "PatchSession",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "d64650fb9560",
      "file": "iznik-server-go/spammers/spammers.go",
      "function": "GetSpammers",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "0ca4810292dc",
      "file": "iznik-server-go/story/story.go",
      "function": "List",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "4853849663f1",
      "file": "iznik-server-go/user/authMiddleware.go",
      "function": "NewAuthMiddleware",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "e04bf70e7bee",
      "file": "iznik-server-go/user/authMiddleware.go",
      "function": "NewAuthMiddleware",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "3ecb2fba572f",
      "file": "iznik-server-go/authority/stats.go",
      "function": "GetStatsByAuthority",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "6d50d3895aa7",
      "file": "iznik-server-go/authority/stats.go",
      "function": "GetStatsByAuthority",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "f281cfe83025",
      "file": "iznik-server-go/authority/stats.go",
      "function": "GetStatsByAuthority",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "ff9be67577e8",
      "file": "iznik-server-go/message/reach.go",
      "function": "ReachBlockedSet",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a literal value, column name, table name, WHERE fragment, or whole clause assembled at request time via fmt.Sprintf/string concatenation, captured by the extractor as \"{{expr}}\"/\"%s\"/\"%d\"/\"%f\" placeholders in the source text rather than as bind parameters). No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "19b190d43a85",
      "file": "iznik-server-go/changes/changes.go",
      "function": "GetChanges",
      "reason": "Top-level UNION of multiple SELECTs with no outer FROM to attach a builder to (unlike the derived-table-wrapped UNIONs converted elsewhere in this wave, e.g. group/groupWork.go's GetGroupWork). GORM's chainable API has no UNION clause, and .Table()/.Joins()/.Where() all assume a single FROM target, so there is no way to construct this statement through the established builder pattern.",
      "porting": "PostgreSQL: UNION and UNION ALL are standard and port unchanged. Raw here because GORM ships no UNION clause, not because of dialect.",
      "category": "Top-level UNION"
    },
    {
      "id": "e9ea468dab80",
      "file": "iznik-server-go/modconfig/modconfig.go",
      "function": "listModConfigs",
      "reason": "Top-level UNION of multiple SELECTs with no outer FROM to attach a builder to (unlike the derived-table-wrapped UNIONs converted elsewhere in this wave, e.g. group/groupWork.go's GetGroupWork). GORM's chainable API has no UNION clause, and .Table()/.Joins()/.Where() all assume a single FROM target, so there is no way to construct this statement through the established builder pattern.",
      "porting": "PostgreSQL: UNION and UNION ALL are standard and port unchanged. Raw here because GORM ships no UNION clause, not because of dialect.",
      "category": "Top-level UNION"
    },
    {
      "id": "bf1cd8bf4627",
      "file": "iznik-server-go/session/merge.go",
      "function": "mergeChatRooms",
      "reason": "Multi-table UPDATE ... JOIN ... SET (MySQL-specific syntax, updating one aliased table's columns from a value computed via a joined second copy of the same table). GORM's Update/Updates builder targets a single table with a plain SET list; it has no clause for a JOIN inside a write statement, so this cannot be expressed through .Table()/.Update() the way single-table writes in earlier waves were.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "85f3246d11af",
      "file": "iznik-server-go/message/bulkEdit.go",
      "function": "recordBulkOutcomeIfComplete",
      "reason": "INSERT ... SELECT. No GORM builder keeps an INSERT and its source SELECT as one atomic statement - .Create() takes a map/struct of literal values, not a subquery, so there is no way to express this without splitting it into a read then a write, which would open a race the single statement does not have.",
      "porting": "PostgreSQL: INSERT INTO ... SELECT is standard and ports unchanged, but check any ON DUPLICATE KEY UPDATE tail, which becomes ON CONFLICT ... DO UPDATE and needs a named unique index or constraint on the conflict target - Postgres will not infer it.",
      "category": "INSERT ... SELECT"
    },
    {
      "id": "56c15677ea5b",
      "file": "iznik-server-go/session/session.go",
      "function": "PatchSession",
      "reason": "INSERT ... SELECT. No GORM builder keeps an INSERT and its source SELECT as one atomic statement - .Create() takes a map/struct of literal values, not a subquery, so there is no way to express this without splitting it into a read then a write, which would open a race the single statement does not have.",
      "porting": "PostgreSQL: INSERT INTO ... SELECT is standard and ports unchanged, but check any ON DUPLICATE KEY UPDATE tail, which becomes ON CONFLICT ... DO UPDATE and needs a named unique index or constraint on the conflict target - Postgres will not infer it.",
      "category": "INSERT ... SELECT"
    },
    {
      "id": "cdbd40c98336",
      "file": "iznik-server-go/authority/stats.go",
      "function": "GetStatsByAuthority",
      "reason": "DROP TEMPORARY TABLE - a DDL statement, not a query or write GORM's clause-based builder models at all (no Table/Select/Where/Create/Update/Delete shape applies to a bare DDL statement). Session-scoped alongside the CREATE TEMPORARY TABLE it pairs with in the same function; see GetStatsByAuthority's dbresolver.Write-pinned transaction comment for why the pairing matters.",
      "porting": "PostgreSQL: CREATE TEMPORARY TABLE becomes CREATE TEMP TABLE (or keep TEMPORARY, both are accepted). The behavioural difference that matters: Postgres temp tables are per-SESSION and dropped at session end, and CREATE TABLE ... AS does not accept MySQL's index hints. The surrounding code already pins one connection for the temp table's lifetime, which is what makes this work on either engine.",
      "category": "DDL (temporary tables)"
    },
    {
      "id": "dd475ec31446",
      "file": "iznik-server-go/authority/stats.go",
      "function": "GetStatsByAuthority",
      "reason": "DROP TEMPORARY TABLE - a DDL statement, not a query or write GORM's clause-based builder models at all (no Table/Select/Where/Create/Update/Delete shape applies to a bare DDL statement). Session-scoped alongside the CREATE TEMPORARY TABLE it pairs with in the same function; see GetStatsByAuthority's dbresolver.Write-pinned transaction comment for why the pairing matters.",
      "porting": "PostgreSQL: CREATE TEMPORARY TABLE becomes CREATE TEMP TABLE (or keep TEMPORARY, both are accepted). The behavioural difference that matters: Postgres temp tables are per-SESSION and dropped at session end, and CREATE TABLE ... AS does not accept MySQL's index hints. The surrounding code already pins one connection for the temp table's lifetime, which is what makes this work on either engine.",
      "category": "DDL (temporary tables)"
    },
    {
      "id": "8d359d691beb",
      "file": "iznik-server-go/message/helper.go",
      "function": "helperResolveProposal",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" (confirmed by rendering gorm.io/gorm/clause's own insert_test.go cases: Modifier:\"LOW_PRIORITY\" renders \"INSERT LOW_PRIORITY INTO ...\") - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "09730933af57",
      "file": "iznik-server-go/newsfeed/newsfeed.go",
      "function": "Post",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" (confirmed by rendering gorm.io/gorm/clause's own insert_test.go cases: Modifier:\"LOW_PRIORITY\" renders \"INSERT LOW_PRIORITY INTO ...\") - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "1ff179ae0e2c",
      "file": "iznik-server-go/newsfeed/newsfeed.go",
      "function": "Post",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" (confirmed by rendering gorm.io/gorm/clause's own insert_test.go cases: Modifier:\"LOW_PRIORITY\" renders \"INSERT LOW_PRIORITY INTO ...\") - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "f3a8b6237a60",
      "file": "iznik-server-go/team/team.go",
      "function": "PatchTeam",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" (confirmed by rendering gorm.io/gorm/clause's own insert_test.go cases: Modifier:\"LOW_PRIORITY\" renders \"INSERT LOW_PRIORITY INTO ...\") - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "8ec21db34aa4",
      "file": "iznik-server-go/amp/amp.go",
      "function": "ValidateToken",
      "reason": "Bare SELECT EXISTS(...) with no top-level FROM clause at all - the whole statement is one scalar expression. GORM's chainable builder always renders a FROM clause once .Table() (or .Model()) is called, so there is no way to construct \"SELECT EXISTS(...)\" with nothing else through the established Table()/Select()/Where() pattern without adding a FROM this golden does not have.",
      "porting": "PostgreSQL: SELECT EXISTS(SELECT 1 FROM ...) is standard and ports unchanged. The reason this is raw is a GORM limitation (its builder always emits a FROM once Table() is called), not a dialect one, so the port is a no-op.",
      "category": "Bare SELECT EXISTS, no top-level FROM"
    },
    {
      "id": "faf666d9cb13",
      "file": "iznik-server-go/amp/amp.go",
      "function": "validateBodyToken",
      "reason": "Bare SELECT EXISTS(...) with no top-level FROM clause at all - the whole statement is one scalar expression. GORM's chainable builder always renders a FROM clause once .Table() (or .Model()) is called, so there is no way to construct \"SELECT EXISTS(...)\" with nothing else through the established Table()/Select()/Where() pattern without adding a FROM this golden does not have.",
      "porting": "PostgreSQL: SELECT EXISTS(SELECT 1 FROM ...) is standard and ports unchanged. The reason this is raw is a GORM limitation (its builder always emits a FROM once Table() is called), not a dialect one, so the port is a no-op.",
      "category": "Bare SELECT EXISTS, no top-level FROM"
    },
    {
      "id": "848af7d73bfe",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "recordReplyAttribution",
      "reason": "Bare SELECT EXISTS(...) with no top-level FROM clause at all - the whole statement is one scalar expression. GORM's chainable builder always renders a FROM clause once .Table() (or .Model()) is called, so there is no way to construct \"SELECT EXISTS(...)\" with nothing else through the established Table()/Select()/Where() pattern without adding a FROM this golden does not have. (wasHome: established-membership check for the reply-attribution ladder.)",
      "porting": "PostgreSQL: SELECT EXISTS(SELECT 1 FROM ...) is standard and ports unchanged. The reason this is raw is a GORM limitation (its builder always emits a FROM once Table() is called), not a dialect one, so the port is a no-op.",
      "category": "Bare SELECT EXISTS, no top-level FROM"
    },
    {
      "id": "582f5b4bb7ce",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "recordReplyAttribution",
      "reason": "Bare SELECT EXISTS(...) with no top-level FROM clause at all - the whole statement is one scalar expression. GORM's chainable builder always renders a FROM clause once .Table() (or .Model()) is called, so there is no way to construct \"SELECT EXISTS(...)\" with nothing else through the established Table()/Select()/Where() pattern without adding a FROM this golden does not have. (wasNotified: rippling_reach_notified ledger lookup.)",
      "porting": "PostgreSQL: SELECT EXISTS(SELECT 1 FROM ...) is standard and ports unchanged. The reason this is raw is a GORM limitation (its builder always emits a FROM once Table() is called), not a dialect one, so the port is a no-op.",
      "category": "Bare SELECT EXISTS, no top-level FROM"
    },
    {
      "id": "fc0c6fd4f6df",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "recordReplyAttribution",
      "reason": "Bare SELECT EXISTS(...) with no top-level FROM clause at all - the whole statement is one scalar expression. GORM's chainable builder always renders a FROM clause once .Table() (or .Model()) is called, so there is no way to construct \"SELECT EXISTS(...)\" with nothing else through the established Table()/Select()/Where() pattern without adding a FROM this golden does not have. (wasRippleGroup: rippled-in membership check.)",
      "porting": "PostgreSQL: SELECT EXISTS(SELECT 1 FROM ...) is standard and ports unchanged. The reason this is raw is a GORM limitation (its builder always emits a FROM once Table() is called), not a dialect one, so the port is a no-op.",
      "category": "Bare SELECT EXISTS, no top-level FROM"
    },
    {
      "id": "461f55d25b16",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "recordReplyAttribution",
      "reason": "Bare SELECT EXISTS(...) with no top-level FROM clause at all - the whole statement is one scalar expression. GORM's chainable builder always renders a FROM clause once .Table() (or .Model()) is called, so there is no way to construct \"SELECT EXISTS(...)\" with nothing else through the established Table()/Select()/Where() pattern without adding a FROM this golden does not have. (postHadRippled: has the post rippled at all.)",
      "porting": "PostgreSQL: SELECT EXISTS(SELECT 1 FROM ...) is standard and ports unchanged. The reason this is raw is a GORM limitation (its builder always emits a FROM once Table() is called), not a dialect one, so the port is a no-op.",
      "category": "Bare SELECT EXISTS, no top-level FROM"
    },
    {
      "id": "a4250aa0ada3",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "recordReplyAttribution",
      "reason": "Bare SELECT EXISTS(...) with no top-level FROM clause at all - the whole statement is one scalar expression. GORM's chainable builder always renders a FROM clause once .Table() (or .Model()) is called, so there is no way to construct \"SELECT EXISTS(...)\" with nothing else through the established Table()/Select()/Where() pattern without adding a FROM this golden does not have. (hasReach: rippling_reach existence fallback.)",
      "porting": "PostgreSQL: SELECT EXISTS(SELECT 1 FROM ...) is standard and ports unchanged. The reason this is raw is a GORM limitation (its builder always emits a FROM once Table() is called), not a dialect one, so the port is a no-op.",
      "category": "Bare SELECT EXISTS, no top-level FROM"
    },
    {
      "id": "4fc47623d055",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "recordReplyAttribution",
      "reason": "Bare SELECT EXISTS(...) with no top-level FROM clause at all - the whole statement is one scalar expression. GORM's chainable builder always renders a FROM clause once .Table() (or .Model()) is called, so there is no way to construct \"SELECT EXISTS(...)\" with nothing else through the established Table()/Select()/Where() pattern without adding a FROM this golden does not have. (inOrigin: also spatial - ST_Contains/ST_GeometryType/ST_SRID inside the subquery - but the FROM-less shape is the sole reason it stays raw, not the spatial content, which is otherwise ordinary literal text.)",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "a74101bfbfa2",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "CreateChatMessage",
      "reason": "Bare SELECT EXISTS(...) with no top-level FROM clause at all - the whole statement is one scalar expression. GORM's chainable builder always renders a FROM clause once .Table() (or .Model()) is called, so there is no way to construct \"SELECT EXISTS(...)\" with nothing else through the established Table()/Select()/Where() pattern without adding a FROM this golden does not have. (refExists: guards a reply against a purged refmsgid.)",
      "porting": "PostgreSQL: SELECT EXISTS(SELECT 1 FROM ...) is standard and ports unchanged. The reason this is raw is a GORM limitation (its builder always emits a FROM once Table() is called), not a dialect one, so the port is a no-op.",
      "category": "Bare SELECT EXISTS, no top-level FROM"
    },
    {
      "id": "c9ff161437b9",
      "file": "iznik-server-go/message/message.go",
      "function": "GetMessagesByIds",
      "reason": "Bare SELECT EXISTS(...) with no top-level FROM clause at all - the whole statement is one scalar expression. GORM's chainable builder always renders a FROM clause once .Table() (or .Model()) is called, so there is no way to construct \"SELECT EXISTS(...)\" with nothing else through the established Table()/Select()/Where() pattern without adding a FROM this golden does not have. (SELECT EXISTS(SELECT 1 FROM rippling_reach WHERE msgid IN (?)).)",
      "porting": "PostgreSQL: SELECT EXISTS(SELECT 1 FROM ...) is standard and ports unchanged. The reason this is raw is a GORM limitation (its builder always emits a FROM once Table() is called), not a dialect one, so the port is a no-op.",
      "category": "Bare SELECT EXISTS, no top-level FROM"
    },
    {
      "id": "67871ee12b8f",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "holdChatMessage",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "db4ec8586401",
      "file": "iznik-server-go/message/message.go",
      "function": "handleApprove",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "049bc3f964cd",
      "file": "iznik-server-go/message/message.go",
      "function": "handleSpam",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "7efd04588fb2",
      "file": "iznik-server-go/message/message.go",
      "function": "handlePromise",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "33201edc2a80",
      "file": "iznik-server-go/user/user.go",
      "function": "handleRate",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "25b7b92e33fd",
      "file": "iznik-server-go/location/location.go",
      "function": "CreateLocation",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword. Also carries a second, independent blocker: the SRID is embedded via fmt.Sprintf(..., utils.SRID) as a literal %d BEFORE the string ever reaches Exec, so the manifest's recorded goldenSql is unresolved template text (\"ST_GeomFromText(?, %d)\") rather than real SQL - there is nothing for a rendered statement to compare against even setting the REPLACE issue aside.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "6f1d6543e5c0",
      "file": "iznik-server-go/location/location.go",
      "function": "UpdateLocation",
      "reason": "REPLACE INTO cannot be expressed via GORM's clause.Insert. The framework writes the clause's Name() (\"INSERT\") before calling Build(), and Insert.Build() only ever appends a Modifier word plus \"INTO\" after that literal \"INSERT\" - so Modifier:\"REPLACE\" would render \"INSERT REPLACE INTO ...\", not \"REPLACE INTO ...\". There is no clause that swaps the statement keyword itself, unlike INSERT IGNORE (wave 3's clause.Insert{Modifier:\"IGNORE\"}) which only ever needed a modifier, not a different keyword. Also carries a second, independent blocker: the SRID is embedded via fmt.Sprintf(..., utils.SRID) as a literal %d BEFORE the string ever reaches Exec, so the manifest's recorded goldenSql is unresolved template text (\"ST_GeomFromText(?, %d)\") rather than real SQL - there is nothing for a rendered statement to compare against even setting the REPLACE issue aside.",
      "porting": "PostgreSQL: INSERT ... ON CONFLICT (cols) DO UPDATE. Note this is NOT a behavioural equivalent. MySQL REPLACE deletes the conflicting row and inserts a new one, which fires DELETE triggers, cascades foreign keys, and assigns a NEW auto-increment id. ON CONFLICT updates in place, keeping the row and its id. Anything depending on the row identity changing, or on ON DELETE CASCADE firing, changes behaviour at the port.",
      "category": "REPLACE INTO"
    },
    {
      "id": "d4f71ea17664",
      "file": "iznik-server-go/message/message.go",
      "function": "ClipReachForRejectedGroup",
      "reason": "Multi-table DELETE ... JOIN ... WHERE (MySQL-specific syntax: \"DELETE mr FROM rippling_reach mr JOIN `groups` g ON ...\"). GORM's Delete callback (callbacks/delete.go) adds a bare clause.From{} with no Joins populated from Statement.Joins - unlike the SELECT query callback, which explicitly merges Statement.Joins into the From clause - so a .Joins() call before .Delete() is silently dropped rather than rendered, producing a DELETE that omits the second table entirely. Also spatial (ST_GeometryType/ST_Within), per plan 7.5, though the JOIN gap is the structural blocker.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "8c15ce918aa2",
      "file": "iznik-server-go/message/message.go",
      "function": "PutMessageAs",
      "reason": "Multi-table UPDATE ... JOIN ... JOIN ... SET (MySQL-specific syntax, updating messages from two joined tables: users and locations). GORM's Update/Updates builder targets a single table with a plain SET list (mysql.go's UpdateClauses = [UPDATE, SET, WHERE, ORDER BY, LIMIT] - no FROM/JOIN entry at all); it has no clause for a JOIN inside a write statement, so this cannot be expressed through .Table()/.Update() the way single-table writes in earlier waves were.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "a79f147726d0",
      "file": "iznik-server-go/user/user.go",
      "function": "handleMerge",
      "reason": "Multi-table UPDATE ... JOIN ... SET (MySQL-specific syntax: UPDATE memberships m2 JOIN memberships m1 ON ... SET m2.added = LEAST(m2.added, m1.added), updating one aliased copy of memberships from a value computed via a second joined copy of the same table). GORM's Update/Updates builder targets a single table with a plain SET list; it has no clause for a JOIN inside a write statement.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "5b72a0acad8e",
      "file": "iznik-server-go/user/user.go",
      "function": "handleMerge",
      "reason": "Multi-table UPDATE ... JOIN ... SET (MySQL-specific syntax: UPDATE users u2 JOIN users u1 ON ... SET u2.added = LEAST(u2.added, u1.added), the users-table twin of this same function's memberships merge, a79f147726d0). GORM's Update/Updates builder targets a single table with a plain SET list; it has no clause for a JOIN inside a write statement.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "4f9d823a53a7",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "handleAllSeen",
      "reason": "INSERT ... SELECT: seeds a chat_roster row for every chat_rooms row the user participates in that doesn't already have one, in one statement (INSERT INTO chat_roster (...) SELECT cr.id, ?, ... FROM chat_rooms cr WHERE ... AND NOT EXISTS (...)). GORM has no builder that expresses INSERT ... SELECT as one atomic statement - Create takes values, not a query - so converting it would mean reading the candidate rows and inserting them separately, a different statement with a different concurrency story, not a port.",
      "porting": "PostgreSQL: INSERT INTO ... SELECT is standard and ports unchanged, but check any ON DUPLICATE KEY UPDATE tail, which becomes ON CONFLICT ... DO UPDATE and needs a named unique index or constraint on the conflict target - Postgres will not infer it.",
      "category": "INSERT ... SELECT"
    },
    {
      "id": "1d98111ea90d",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "handleAllSeen",
      "reason": "INSERT ... SELECT, the ModTools-branch twin of this same function's 4f9d823a53a7 (chat_rooms.id IN (...) instead of the FD branch's user1/user2 pair). Also runtime-varying: the IN-list is a literal-value list built by joining formatted chat ids (joinIDs), not a bind, so even the INSERT ... SELECT issue aside there is no fixed golden - the manifest records it as {{expr}}.",
      "porting": "PostgreSQL: INSERT INTO ... SELECT is standard and ports unchanged, but check any ON DUPLICATE KEY UPDATE tail, which becomes ON CONFLICT ... DO UPDATE and needs a named unique index or constraint on the conflict target - Postgres will not infer it.",
      "category": "INSERT ... SELECT"
    },
    {
      "id": "33ad97a3417c",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "CreateChatMessage",
      "reason": "Top-level UNION of three SELECTs (by id/user1, by id/user2, and a memberships-joined moderator check) with no outer FROM to attach a builder to - unlike the derived-table-wrapped UNIONs converted elsewhere in this wave (e.g. user/user.go's SearchUsers, 0c9e859752d0, and GetSearchesForUser, 06d02e94fa4a, both of which have an outer \"SELECT ... FROM (...) t\" shape and so go through .Table(\"(...) t\", args...) instead). GORM's chainable API has no UNION clause, and .Table()/.Joins()/.Where() all assume a single FROM target, so a bare UNION with nothing wrapping it cannot be constructed through the established builder pattern.",
      "porting": "PostgreSQL: UNION and UNION ALL are standard and port unchanged. Raw here because GORM ships no UNION clause, not because of dialect.",
      "category": "Top-level UNION"
    },
    {
      "id": "3736547cd88a",
      "file": "iznik-server-go/location/location.go",
      "function": "ClosestGroups",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (haversine/ST_Contains/ST_distance/MBRIntersects/ST_SRID/POLYGON/LINESTRING), the category plan 7.5 names explicitly. This is the core group/location MATCHING and RANKING logic (which real groups or locations a point falls near or inside), the same kind of \"wrong nearby X\" risk that message/bounds.go and message/search.go were kept raw for in wave 1 - a rewrite bug here changes which groups/locations show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically even though the text is otherwise renderable. (Containment pass: ST_Contains(polyindex, ...) plus haversine distance ranking.)",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "961c4c5a214a",
      "file": "iznik-server-go/location/location.go",
      "function": "ClosestGroups",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (haversine/ST_Contains/ST_distance/MBRIntersects/ST_SRID/POLYGON/LINESTRING), the category plan 7.5 names explicitly. This is the core group/location MATCHING and RANKING logic (which real groups or locations a point falls near or inside), the same kind of \"wrong nearby X\" risk that message/bounds.go and message/search.go were kept raw for in wave 1 - a rewrite bug here changes which groups/locations show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically even though the text is otherwise renderable. (Radius-band pass: ST_distance/MBRIntersects over an expanding bounding box, run per band from a goroutine.)",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "dc731a47a66a",
      "file": "iznik-server-go/location/location.go",
      "function": "SearchLocations",
      "reason": "Spatial: uses MySQL ST_*/MBR* geometry functions (haversine/ST_Contains/ST_distance/MBRIntersects/ST_SRID/POLYGON/LINESTRING), the category plan 7.5 names explicitly. This is the core group/location MATCHING and RANKING logic (which real groups or locations a point falls near or inside), the same kind of \"wrong nearby X\" risk that message/bounds.go and message/search.go were kept raw for in wave 1 - a rewrite bug here changes which groups/locations show up as nearby with no obviously wrong shape to the query, so it is not converted mechanically even though the text is otherwise renderable. (Bounding-box intersection query for the ModTools area-boundary editor - Discourse #9770 is a real production bug from a previous subtle change to this exact query's geometry handling.)",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "f557717fbfce",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "FetchChatMessages",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a review-vs-non-review WHERE fragment (reviewFilter), a soft-deleted-sender filter only added for non-mod callers (deletedFilter), an optional \"AND id != ?\" exclusion, an ASC/DESC ORDER BY branch, and a LIMIT embedded via fmt.Sprintf as literal text rather than a bind), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "7470288c95b7",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "FetchChatMessages",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the IN-list is a literal-value list built by strings.Join(fmt.Sprintf(\"%d\", id), \",\"), not a bind, so the number and text of the placeholders inside IN(...) varies with how many message ids are held), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "67cd5e1cc4ec",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "CreateChatMessage",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the aggregate expression is rippling.ReachInReachExpr()'s return value, a Go function call the extractor cannot fold to a static golden (unlike a plain string constant, which a later extractor change now resolves for wave-1 sites such as modconfig.go's configColumns)), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "07113a2db28b",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "getChatMessagesForRoom",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (a participant-vs-moderator WHERE fragment (reviewFilter), an optional soft-deleted-sender filter, and an optional pagination cutoff embedded via string concatenation of a formatted uint64 rather than a bind (ctxq)), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "1ff296c8656c",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "getReviewQueue",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the statement is baseQuery+widerQuery, both built from recipientExpr (a CASE expression assembled elsewhere in the function) and a literal-value group-id list, neither of which is a fixed constant), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "5da587b4234d",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "getReviewQueue",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the non-wider-review branch of the same baseQuery this function's 1ff296c8656c also uses, which embeds recipientExpr (a CASE expression assembled at runtime) directly into the SQL text), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "62a2f6fa4bdb",
      "file": "iznik-server-go/chat/chatmessage.go",
      "function": "getReviewQueue",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the IN-list is a literal-value list built by strings.Join over formatted user ids, not a bind, so the number and text of the placeholders inside IN(...) varies with how many held-by users are being resolved), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "537a2764efde",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "listChats",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the statement is assembled from a variable-length `unions` slice (one UNION arm appended per matching chat-room shape) joined with \" UNION \", so both the number of arms and the final SQL text vary per call), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: UNION and UNION ALL are standard and port unchanged. Raw here because GORM ships no UNION clause, not because of dialect.",
      "category": "Top-level UNION"
    },
    {
      "id": "b184ef8e3ad2",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "listChats",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the statement embeds a WITH-CTE and a literal-value id list (idlist, built by strings.Join over formatted ids) directly into the SQL text, neither of which the extractor can fold to a fixed golden), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "366c64b4d972",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "listChats",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the IN-list (idlist) is a literal-value list built by strings.Join over formatted user ids, not a bind, so the text varies with how many users are being checked for supporter status), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "3519e352775b",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "handleAllSeen",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the chatid IN(...) list (idlist) is a literal-value list built by strings.Join over formatted chat ids, not a bind, so the text varies with how many moderator chats the caller has), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "fdeb2007824a",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "countUnseenMT",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the chat_messages.chatid IN(...) list (idlist, via joinIDs) is a literal-value list, not a bind, so the text varies with how many moderator chat ids are being counted), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "35023816be21",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "getModeratorChatIDs",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the \"active\" membership filter (activeq) is only appended when the search parameter is empty, so the statement has two different shapes depending on the caller), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "e99680f74b2e",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "getModeratorChatIDs",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the User2Mod branch's twin of this same function's 35023816be21: the \"active\" membership filter (activeq) is only appended when the search parameter is empty), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "d44b753b35c8",
      "file": "iznik-server-go/chat/chatroom.go",
      "function": "getModeratorChatIDs",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the chat_rooms.id IN(...) list (idlist) is a literal-value list built by strings.Join over formatted chat ids, not a bind, so the text varies with how many candidate chat ids the search is narrowing), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "e1daa6fea45a",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "getBouncesEmailsStats",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the \"AND date BETWEEN ? AND ?\" clause is only appended when both startDate and endDate are supplied, so the statement has two different shapes depending on the caller's query params), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "52ecf7eb71ac",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "getAMPStats",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the WHERE conditions string is built by conditionally appending an email_type filter and/or a date-range filter before this AMP-emails query runs, so up to four different shapes are possible), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "c8a4c6cbcae8",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "getAMPStats",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the non-AMP-emails twin of this same function's 52ecf7eb71ac, sharing the same conditionally-built WHERE conditions string), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "ef321afa5b7a",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "getAMPStats",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the click-breakdown query reuses the same conditionally-built WHERE conditions string (via string-replace) as 52ecf7eb71ac, so it inherits the same variable shape), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "b37a229bfb0b",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "getAMPStats",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the non-AMP click-breakdown twin of this same function's ef321afa5b7a, sharing the same conditionally-built WHERE conditions string), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "69f6acdc5a6b",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "TimeSeries",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the daily-stats query conditionally appends \"AND email_type = ?\" before its GROUP BY/ORDER BY, so the statement has two different shapes depending on the caller's query params), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "9d115fb3ebcd",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "TimeSeries",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the extractor cannot fold the local `bounceQuery` variable to a fixed golden (goldenSql records \"{{built at runtime: bounceQuery}}\") - the constant-folding a later extractor change added for wave-1 sites covers package-level `const` declarations (e.g. modconfig.go's configColumns), not a function-local variable, even though this particular one is never reassigned), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "ecbedcafc048",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "StatsByType",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the query string is conditionally assembled (type/date-range filters) before this stats-by-type breakdown runs, the same pattern as getAMPStats's conditions), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "f6e4a52a0fb6",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "TopClickedLinks",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the query string is conditionally assembled (type/date-range filters) before this top-clicked-links breakdown runs, the same pattern as getAMPStats's conditions), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "284dfbc44866",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "DigestClickPositions",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the extractor cannot fold the local `denomQuery` variable to a fixed golden (goldenSql records \"{{built at runtime: denomQuery}}\"), the same function-local-variable gap as TimeSeries's bounceQuery (9d115fb3ebcd)), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "dce69264c9c0",
      "file": "iznik-server-go/emailtracking/emailtracking.go",
      "function": "DigestClickPositions",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the extractor cannot fold the local `clickQuery` variable to a fixed golden (goldenSql records \"{{built at runtime: clickQuery}}\"), the same function-local-variable gap as this function's own denomQuery (284dfbc44866)), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "b262bf75df3c",
      "file": "iznik-server-go/location/location.go",
      "function": "SearchLocations",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (an optional area-scoping WHERE fragment ({{expr}}) is appended only when an area filter is supplied, so the statement has two different shapes depending on the caller), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "71f1772f4a99",
      "file": "iznik-server-go/location/location.go",
      "function": "Typeahead",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (shares SearchLocations's pattern: an optional area-scoping WHERE fragment ({{expr}}) is appended only when an area filter is supplied), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "08bb471351a0",
      "file": "iznik-server-go/message/message.go",
      "function": "GetMessagesByIds",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the SELECT list and WHERE are assembled from several optional fragments depending on which fields/filters the caller requested, so the statement has more than one possible shape), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "2de07c2af78b",
      "file": "iznik-server-go/message/message.go",
      "function": "GetMessagesForUser",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the extractor cannot fold the local `sql` variable to a fixed golden (goldenSql records \"{{built at runtime: sql}}\") - it is assembled from several optional filter fragments across the function), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "bca1186d1ea4",
      "file": "iznik-server-go/message/message.go",
      "function": "GetMessagesForUser",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the second use of the same runtime-built `sql` variable this function's 2de07c2af78b also cannot resolve), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "7653c7a2e4ed",
      "file": "iznik-server-go/message/message.go",
      "function": "ClipReachForRejectedGroup",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (an optional \", mr.inner_bound = NULL\" SET fragment ({{expr}}) is appended only when rippling.ReachBoundsReady(db) is true, so the statement has two different shapes depending on runtime schema-migration state - and it is also a multi-table UPDATE ... JOIN (see d4f71ea17664's DELETE twin in this same function for that mechanism)), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "99713f48c505",
      "file": "iznik-server-go/message/message.go",
      "function": "handleRevertEdits",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the SET list ({{expr}}) is built by joining a variable number of \"col = ?\" fragments, one per field the revert actually changes, so both the column list and the placeholder count vary per call), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "e9f2c662be69",
      "file": "iznik-server-go/message/message.go",
      "function": "applyPatchMessageCore",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the SET list ({{expr}}) is built by joining a variable number of \"col = ?\" fragments, one per field present in the PATCH request, so both the column list and the placeholder count vary per call), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "e0558c2c039d",
      "file": "iznik-server-go/user/user.go",
      "function": "GetUserById",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the SELECT list includes an optional fragment ({{expr}}) added only for certain caller/target combinations, so the statement has more than one possible shape), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "941509171a6e",
      "file": "iznik-server-go/user/user.go",
      "function": "PatchUser",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the SET list ({{expr}}) is built by joining a variable number of \"col = ?\" fragments, one per field present in the PATCH request, so both the column list and the placeholder count vary per call), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: this is a genuine rewrite, not a translation. MySQL's UPDATE a JOIN b ON ... SET a.x = ... becomes UPDATE a SET x = ... FROM b WHERE <join cond>, and DELETE a FROM a JOIN b becomes DELETE FROM a USING b WHERE <join cond>. The join condition moves out of ON and into WHERE, and a mistake there silently widens the write to every row.",
      "category": "Multi-table UPDATE/DELETE ... JOIN"
    },
    {
      "id": "d5ecca066b1b",
      "file": "iznik-server-go/user/user.go",
      "function": "handleMerge",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the extractor cannot fold the local `u.sql` variable to a fixed golden (goldenSql records \"{{built at runtime: u.sql}}\") - it is assembled from several optional merge-step fragments), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "395499023142",
      "file": "iznik-server-go/user/user.go",
      "function": "GetUserReplies",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the extractor cannot fold the local `query` variable to a fixed golden (goldenSql records \"{{built at runtime: query}}\") - it is assembled from several optional filter fragments), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: no single translation - the statement is assembled at runtime, so each shape needs its own review. The SQL inside is generally ordinary, so the port cost is in reading the builder rather than in dialect features. Worth reviewing these for whether the dynamic assembly is still needed at all, since several build a WHERE from a fixed set of options that a query builder could express directly.",
      "category": "Runtime-varying SQL"
    },
    {
      "id": "745c0a9ca82e",
      "file": "iznik-server-go/location/location.go",
      "function": "UpdateLocation",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the SRID is embedded via fmt.Sprintf(\"SELECT ST_IsValid(ST_GeomFromText(?, %d)) AS valid\", utils.SRID) as a literal %d BEFORE the string ever reaches Raw, so the manifest's recorded goldenSql is unresolved template text rather than real SQL - there is nothing for a rendered statement to compare against), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    },
    {
      "id": "42f88b0d5032",
      "file": "iznik-server-go/location/location.go",
      "function": "UpdateLocation",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the SRID is embedded via fmt.Sprintf(..., utils.SRID, utils.SRID) as two literal %d verbs in the old/new-geometry union query BEFORE the string ever reaches Raw, so the manifest's recorded goldenSql is unresolved template text rather than real SQL), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL: UNION and UNION ALL are standard and port unchanged. Raw here because GORM ships no UNION clause, not because of dialect.",
      "category": "Top-level UNION"
    },
    {
      "id": "aa63c688e6b1",
      "file": "iznik-server-go/location/location.go",
      "function": "UpdateLocation",
      "reason": "Runtime-varying: the statement has more than one possible rendered form (the SRID is embedded via fmt.Sprintf(\"UPDATE locations SET `type` = 'Polygon', ourgeometry = ST_GeomFromText(?, %d) WHERE id = ?\", utils.SRID) as a literal %d BEFORE the string ever reaches Exec, so the manifest's recorded goldenSql is unresolved template text rather than real SQL), captured by the extractor as an unresolved \"{{expr}}\"/\"%d\" placeholder in the recorded goldenSql rather than as a bind parameter. No single golden can describe every form it can take, so Layer 1 parity - one rendered statement compared against one golden - is impossible by construction.",
      "porting": "PostgreSQL/PostGIS: most ST_* names carry over, but three things change. ST_Distance_Sphere becomes ST_DistanceSphere or a cast to geography; MySQL's axis order for SRID 4326 is lat/lng while PostGIS is lng/lat, which silently transposes coordinates if unhandled; and MBRContains and friends have no PostGIS equivalent - use && (bounding-box overlap) or ST_Intersects. Getting the axis order wrong shows wrong nearby posts without erroring.",
      "category": "Spatial"
    }
  ]
}

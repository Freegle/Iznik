{"_comment":"Sites where tx.Statement.BuildClauses = []string{\"SELECT\"} is legitimate: the Select argument is a scalar EXPRESSION with no top-level FROM, and the override exists only so GORM does not append a FROM the original never had. Each entry needs a justification. This is an allow-list, not a suppression list - if you cannot say why the argument is an expression rather than a statement, it is not eligible.","allowed":{}}

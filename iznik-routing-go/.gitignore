testdata/*.pbf
testdata/*.osm
# Commit the small Bristol extract — it's the fixture the routing tests build
# their road graph from (and the PBF CI's spatial container boots on).
!testdata/bristol.osm.pbf
spatial-server
iznik-routing-go
data/*
# Commit the prebuilt UK deprivation quintile centroids (all four nations) so a
# fresh checkout boots with a baseline; spatial:update-data overwrites it atomically
# when it successfully refetches. The rest of data/ stays runtime-generated.
!data/uk_lsoa_quintile.csv

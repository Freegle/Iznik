# Loki monitoring - allow for backup window (up to 1 hour tolerance)
# Monit checks every 60 seconds, so 60 cycles = 60 minutes tolerance
check host loki with address docker-internal
  if failed
      port 3100
      protocol http
      request "/ready"
      content = "ready"
      timeout 10 seconds
  for 60 cycles
  then alert
